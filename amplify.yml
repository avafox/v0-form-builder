version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Install dependencies with exact versions (security requirement)
        - npm ci --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
